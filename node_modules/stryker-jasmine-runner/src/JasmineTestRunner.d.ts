import { TestRunner, RunResult, RunnerOptions } from 'stryker-api/test_runner';
export default class JasmineTestRunner implements TestRunner {
    private jasmineConfigFile;
    private fileNames;
    private Date;
    constructor(runnerOptions: RunnerOptions);
    run(options: {
        testHooks?: string;
    }): Promise<RunResult>;
    private createJasmineRunner;
    clearRequireCache(): void;
}
